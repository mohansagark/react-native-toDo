import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {flexDirection: 'row'},
  input: {
    flex: 8,
    borderColor: 'grey',
    borderWidth: 1,
    padding: 3,
    borderRadius: 5,
  },
  button: {flex: 2, borderColor: 'blue', borderWidth: 1, borderRadius: 5},
});

export default styles;
